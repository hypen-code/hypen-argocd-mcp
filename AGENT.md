## INSTRUCTIONS
- Use this mcp framework as base `https://github.com/modelcontextprotocol/rust-sdk`. 
- ArgoCD API documentation `https://cd.apps.argoproj.io/swagger-ui`.
- For each develop generate complete test using mock argocd api server, use `https://cd.apps.argoproj.io/swagger-ui` swagger request, response for mock application. 
- Always make sure all functions test covered using mock service.
- Must complete all testing in development to verify working as expected. mcp server must robust, optimized and reliable. 
- MCP server must optimized response for save model context window.
- Update README.md if nessasary. All docs should maintain on docs dir in root dir seaparately.